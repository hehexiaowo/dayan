---
文档版本：v1.0
编制日期：2026-08-13
文档状态：实现现状补充说明（living document）
对照基线：01产品需求设计文档 v4.0 / 02数据库设计文档 v4.1（正文已含 v4.2/v4.3 修订）
权威来源：db/migration/ 实际迁移脚本 + dayan-server 源码 + 四端前端代码
---

# 大雁养老 — 实现现状与变更说明

> ## 阅读约定
>
> 原始 8 份设计文档（`01`~`08`）作为**设计蓝图**原样保留，不作改动。
> 本文档记录 **2026-08-04（设计定版）至 2026-08-13** 期间，项目在实际落地过程中相对设计文档发生的**结构性变更**，作为"实现现状"的权威补充。
>
> **遇冲突时，以本文档为准。** 阅读顺序：先读对应设计文档建立全貌，再以本文档覆盖其中的差异点。

---

## 一、整体架构现状（对照 `03 系统架构设计文档`）

### 1.1 后端工程结构（实际）

实际为 `dayan-server/` Maven 多模块工程（设计文档 03 称父项目为 `dayan-platform`、06 称 `dayan-server`，**实际为 `dayan-server`**）：

| 分组 | 实际内容 |
|---|---|
| `dayan-common/` | **12 个**基础模块：`bom`、`core`、`mybatis`、`redis`、`security`、`log`、`oss`、`sms` 已实现；`mq`、`lbs`、`pay`、`swagger` 为占位（仅 `package-info`） |
| `dayan-modules/` | **17 个**业务域模块（与设计一致） |
| `dayan-starters/` | **6 个** Spring Boot 启动器（见 1.2） |
| `dayan-gateway/` | Spring Cloud Gateway，端口 **8000** |
| `dayan-job/` | 定时任务，端口 **8090**（见 1.3） |

> 设计文档 03 列 8 个公共模块、06 列 11 个，均不完整。实际为 12 个（含 `bom`）。

### 1.2 启动器与端口（实际）

| 启动器 | 端口 | context-path | 状态 |
|---|---|---|---|
| `dayan-admin` | 8080 | `/admin-api` | ✅ 已实现（接入全部 17 模块） |
| `dayan-channel` | 8081 | `/channel-api` | ✅ 已实现（9 模块） |
| `dayan-agent` | 8082 | `/agent-api` | ✅ 已实现（10 模块） |
| `dayan-client` | 8083 | `/client-api` | ✅ 已实现（10 模块） |
| `dayan-supplier` | 8084 | `/supplier-api` | ⏸ 预留（前端未实现） |
| `dayan-distributor` | 8085 | `/distributor-api` | ⏸ 预留（前端未实现） |

每个 Controller 按**受众端**拆分（`controller/admin|channel|agent|client`），由各启动器按端依赖裁剪装载。技术栈：Spring Boot + Spring Cloud Alibaba（Nacos 服务发现/配置）+ MyBatis-Plus + Sa-Token + Druid。

### 1.3 调度框架 ⚠️ 重要修正

**设计文档自相矛盾**：`06 项目开发规范` §1.1 写 `dayan-job` = "XXL-Job/Quartz 执行器"；`03 系统架构` 写 Spring `@Scheduled` + 分布式锁。

**实际现状：Spring `@Scheduled`（`@EnableScheduling`）。未使用 Quartz，未使用 XXL-Job**（`DayanJobApplication` Javadoc 注明"未来可切 XXL-Job"，属预留扩展点）。全工程无 `org.quartz` / `SchedulerFactory` / `com.xxl` 依赖。

`dayan-job` 内 5 个调度器：

| 调度器类 | Cron | 职责 |
|---|---|---|
| `OrderTimeoutScheduler` | 每 5 分钟 | 待支付订单超时取消（ORDER_SM） |
| `EquityExpireScheduler` | 每小时 | 权益上架过期 + 激活态过期（EQUITY_SM） |
| `QuotaResetScheduler` | 每年 1-1 00:00 + 启动补跑 | 年度配额重置（`resetAnnualQuota`） |
| `ReconciliationScheduler` | 每日 03:00 | `finance_flow` vs `finance_bill` 按渠道对账 |
| `ServiceSlaScheduler` | 每小时 | 服务 SLA：待处理>2h→`urgent`；7 天缺回访告警 |

> Quartz 方案曾尝试接入，后因 Spring Boot 配置冲突**回滚废弃**，改用轻量 Spring 原生调度。

### 1.4 消息队列（设计→落地差异）

设计文档 03 选型 RocketMQ 5.1.x，06 `dayan-common-mq` 注释写"RabbitMQ/RocketMQ"二选一。

**实际：`dayan-common-mq` 为占位模块（仅 `package-info`），未实际接入任何 MQ。** 当前跨域异步逻辑通过事务事件（`@TransactionalEventListener(AFTER_COMMIT)`）+ 本地调度实现，无外部 broker 依赖。

### 1.5 前端四端（实际）

| 目录 | 框架 | UI 库 | 开发端口 | 页面规模 |
|---|---|---|---|---|
| `dayan-admin` | Vue 3 + Vite（Web） | Element Plus | 5173 | ~105 页面 |
| `dayan-channel` | Vue 3 + Vite（Web） | Element Plus | 5174 | ~31 页面 |
| `dayan-agent` | uni-app（H5/小程序）+ Vue 3 | uview-ui | 5182 | ~46 页面 |
| `dayan-client` | uni-app（H5/小程序）+ Vue 3 | uview-ui | 5183 | ~33 页面 |

> supplier / distributor 前端未实现，与设计一致。Web 端共享 `api/components/composables/layouts/router/stores/utils` 结构；uni-app 端用 `manifest.json + pages.json + pages/`。

### 1.6 文件存储（设计→已落地）

设计文档 03 §5.6 方案（MinIO 为主 + OSS/COS 备选 + 预签名直传）已落地：`dayan-common-oss` 实现 MinIO **后端流式中转上传 + 代理下载/显示**；DB 存纯 key；admin 端 25 页接入，channel/agent/client 端修复了显示链路。

---

## 二、数据库结构变更（对照 `02 数据库设计文档 v4.1`）

### 2.1 总览

设计基线 **127 表 / 17 域**。实际现状 **129 表**（净增 2）。变化集中在 5 个域：

| 业务域 | 设计(v4.1) | 实际 | 净变化 |
|---|---|---|---|
| park | 15 | 13 | **−2**（大重构） |
| equity | 6 | 5 | **−1**（模板废除） |
| goods | 5 | 6 | **+1**（SKU 重构） |
| service | 7 | 8 | **+1**（+服务项目+配额） |
| agent | 6 | 8 + `learning_content` | **+2**（线索+学习，迁移 43 后 `agent_lead_trace` −1） |
| system | 18 | 17 | **−1**（日志四分净减，迁移 39） |
| content | 5 | 4 | **−1**（分类字典化，迁移 41） |
| tool | 0 | 1 | **+1**（新建域，迁移 42） |
| lead | 0 | 4 | **+4**（新建域，迁移 43） |
| 其余 10 域 | — | 不变 | 0 |

> 其余域（organ 9 / butler 8 / supplier 10 / scene 5 / channel 11 / client 7 / course 3 / order 4 / finance 7 / distributor 1）表数量与设计一致，仅个别列有调整。迁移 38–43 另含 3 处改名与 poster_template 补建，见 §2.7–2.12。

### 2.2 park 域 — 15 → 13（重大重构）

**核心思路：用"统一表 + 类型字段"替代设计文档中"一类资源一张表"的散表方案。**

| 设计文档(v4.1) | 实际现状 | 变更 |
|---|---|---|
| `park_media_image` / `park_media_video` / `park_media_file` / `park_media_vr`（4 张） | `park_asset` | **合并**（`asset_type` 1图/2视频/3文件/4VR + 来源追踪） |
| `park_room_price` / `park_care_price` / `park_food_price` + facility/service 价格（5 张） | `park_pricing` + `park_pricing_item` | **合并**（`charge_type` 区分；明细进 `park_pricing_item`） |
| `park_facility` | `park_facility_type` | **改名** |
| `park_service_item` | `park_service_type` | **改名** |
| `park_info.score_*`（评分内嵌） | `park_score` | **拆表**（独立评分表 + 乐观锁） |
| `park_care_type` / `park_food_type` | 保留不变 | — |
| —（新增） | `park_display_block` | **新增**（C 端详情页通用展示板块：`block_type`+标题+富文本+JSON 图片数组，替代远程库"加列"灾难） |

`park_info` 关键列调整：`score_*` 移出；`longitude`/`latitude` 改 `DECIMAL(10,6)`；新增 `network_tags`（逗号分隔，FIND_IN_SET 查询）、`vital_config`/`care_config`/`sojourn_config`（TEXT JSON 展示配置）。

**实际 park 表（13）**：`park_info`、`park_asset`、`park_facility_type`、`park_service_type`、`park_adviser`、`park_periphery`、`park_room_type`、`park_pricing`、`park_pricing_item`、`park_score`、`park_care_type`、`park_food_type`、`park_display_block`。

### 2.3 equity 域 — 6 → 5（equity_template 废除）

**核心思路：废除"权益模板"概念，权益属性改由"商品配置 + 服务项目"驱动。**

- **`equity_template` 已 DROP**（`23_drop_legacy.sql`）。
- 替代：`goods_equity`（商品级 1:1 权益配置）+ `service_item`（原子服务能力）+ `goods_service_item_rel`（商品↔服务项目 N:M，见 goods 域）。
- **激活链路改键**：`equity_batch` / `equity_depot` / `equity_activate` 的 `template_code` → **`goods_code`**（`22_equity_link_refactor.sql`）。
- `equity_depot` 列调整：删除 `equity_type`/`equity_value`/`use_count`/`max_use_count`；新增 `person_count`/`valid_days`（激活时从 `goods_equity` 快照）。
- `equity_use_person.use_person_id_card` `VARCHAR(20)` → `VARCHAR(128)`（AES-256-GCM 密文 64 字符，`36_widen_encrypted_id_card.sql`）。

**实际 equity 表（5）**：`equity_batch`、`equity_depot`、`equity_activate`、`equity_use_person`、`equity_change_holder`。

> ⚠️ 全库 AES-GCM 加密列陷阱：`id_card`/`phone` 等 PII 列按明文 `varchar(20)` 建表，但密文需 64 字符。`use_person_id_card` 已扩容，其余列未来加密时同样需扩容至 `varchar(128)`。

### 2.4 goods 域 — 5 → 6（SKU 重构）

| 设计文档(v4.1) | 实际现状 | 变更 |
|---|---|---|
| `goods_sku_equity` | `goods_equity` + `goods_service_item_rel` | **删除并替换** |
| `goods_sku_scene` | `goods_scene` | **改名**（去 `_sku_` 中缀，字段不变） |
| `goods_sku_course` | `goods_course` | **改名** |
| `goods_sku_sojourn` | `goods_sojourn` | **改名** |

`goods_info.goods_type`：1权益 / 2场景 / 3课程 / 4旅居。

**实际 goods 表（6）**：`goods_info`、`goods_equity`、`goods_service_item_rel`、`goods_scene`、`goods_course`、`goods_sojourn`。

### 2.5 service 域 — 7 → 8（+服务项目 + 配额模型）

- **新增 `service_item`**：原子服务能力，`item_category` 1=安排权益 / 2=费用权益；`item_subtype`（仅安排类）1旅居 / 2活力长居 / 3照护长居。
- **配额模型**（`24_quota_model.sql`，2026-08-14 与 25 互换编号以保证全新初始化顺序）：
  - `goods_service_item_rel.quota_type`：**1=终身总量 / 2=年度配额**（年度重置）。
  - `service_session` 新增配额快照列：`max_use_count`（激活时从 `rel.quantity` 冻结）、`used_count`、`quota_type`、`quota_reset_year` + 索引 `idx_quota_reset`。
- `service_session.item_code` 新增；`service_type` 放宽为可 NULL。

**实际 service 表（8）**：`service_session`、`service_item`、`service_equity_demand`、`service_equity_solution`、`service_equity_arrange`、`service_equity_followup`、`service_evaluation`、`service_visit_record`。

### 2.6 agent 域 — 6 → 8（+ `learning_content`）

设计文档仅 6 表，实际已扩展为代理人 CRM + 学习中心：

- **新增 `agent_lead`**：代理人线索（潜在客户 CRM，迁移 28）；后续追加访客追踪列 `visitor_token`/`visitor_source`/`wx_nickname`/`wx_avatar`/`last_trace_time`/`trace_count`/`last_trace_type`（迁移 32/33）。
- **新增 `agent_lead_trace`**：线索互动记录（浏览内容/工具/海报，迁移 32）。
- **新增 `learning_content`**：学习中心内容（视频/图文/雁鸣中国，迁移 34）。**全局表，无 `channel_code`、无 `agent_` 前缀**，须加入租户拦截器忽略清单。

**实际 agent 表**：`agent_info`、`agent_account`、`agent_favorite`、`agent_client_rel`、`agent_performance`、`agent_share_record`、`agent_lead`、`agent_lead_trace` + `learning_content`。

> ⚠️ 迁移 43 起 `agent_lead_trace` 已 DROP，访客身份与互动明细迁入新 lead 域，见 §2.12。

### 2.7 命名收口（迁移 38）

记录类表统一 `_record` 后缀，渠道开放平台表归位：

| 原表 | 现表 | 说明 |
|---|---|---|
| `system_service_change_log` | `service_change_record` | 服务变更归服务域，记录非日志 |
| `system_config_log` | `system_config_change_record` | 配置变更记录 |
| `channel_data_sync_log` | `channel_open_sync_record` | 渠道开放平台同步记录 |

### 2.8 日志域四分（迁移 39）

操作/登录/API 日志按前端归属拆四表，不再设独立登录日志：

- **DROP**：`system_operation_log`、`system_login_log`、`旧 system_log_organ`、`旧 system_log_channel`、`system_log_supplier`。
- **CREATE**：`system_log_organ`（核心后台）/ `system_log_channel`（渠道端）/ `system_log_agent`（代理人端）/ `system_log_client`（客户端），统一结构含 `log_type`（操作/登录/API）。

### 2.9 butler 域 — 管家后台账号关联（迁移 40）

- `butler_info` 新增 `account_code`（关联 `organ_account.account_code`，未开通为 NULL）。
- 管家**保留独立账号体系**（`butler_account`，预留给未来管家端）；当前可经 `organ_account` 直接登录 admin：创建管家时填账号即同步开通，或事后走 `POST /admin-api/butler/info/{butlerCode}/account`。
- 配套种子：`organ_department` DEPT_BUTLER（养老管家）+ `organ_role` ROLE_BUTLER / ROLE_BUTLER_SENIOR / ROLE_BUTLER_LEADER。
- 禁用/删除管家会联动禁用其 organ 账号（软联动，失败不阻断主流程）。

### 2.10 content 域 — content_category 字典化（迁移 41）

- **`content_category` 已 DROP**，分类能力由 `system_dict_business`（业务字典）吸收：`dict_type='content_category'`，`category_type`→`dict_value`，`cover_image`/`is_visible`→`extra` JSON。
- `system_dict_business` 新增 `icon` / `extra`（JSON）两列。
- 举一反三同步收口：`goods_info.category_code` / `course_info.category_code`（原悬空自由文本）改为业务字典选项（`goods_category` / `course_category`）；`learning_content.category` 枚举落 `learning_category` 字典（1视频课程/2图文课程/3雁鸣中国）。
- **迁移 44 后续**：admin「内容分类」独立管理页下线（菜单/权限/content:category:* 删除，分类维护收敛到「系统管理 → 字典管理」）；后端 `ContentCategoryAdminController` 及 DTO/VO/Service 全家桶删除，`ContentInfoServiceImpl.listCategoriesByContentCodes` 改直查字典；admin 内容列表/详情的分类下拉改走 `useBusinessDictOptions('content_category')`。

**实际 content 表（4）**：`content_info`、`content_favorite`、`content_share_record`、`content_read_stat`（分类不再独立成表）。

### 2.11 tool 域 — 新建（迁移 42）

- **新增 `tool_info`**：获客工具定义（`tool_code` TL 前缀、`tool_type` 1计算器/2测评/3表单/4其他、`entry_path` 端上入口、`config` JSON、`visible_scope` 逗号分隔可见端）。**全局表无 `channel_code`**，已加入 agent 端租户忽略清单。
- 种子：TL00001 退休养老金计算器、TL00002 养老生活缺口计算器。
- **补建 `poster_template`**（CREATE IF NOT EXISTS）：修复全新初始化缺表问题。

### 2.12 lead 域 — 新建 + agent_lead 降级 CRM（迁移 43）

访客线索从 agent 域独立为 lead 域：lead_info 是**渠道级访客唯一身份**（无账号，渐进补全 openid→手机号→客户关联），互动明细按类型拆三张表。

- **新增 `lead_info`**：`lead_code`（VL 前缀全局唯一）、`visitor_token` uk、`channel_code`（首触渠道）、`source_type`（1内容/2工具/3海报/4直接访问）、`client_code`（留资建档回填）、`last_interact_*` / `interact_count` 聚合列。
- **新增互动记录三表**：`lead_content_read_record` / `lead_tool_use_record` / `lead_poster_view_record`，均带 `agent_code`（分享人，NULL=直接访问）。
- `agent_lead` 新增 `visitor_lead_code`（认领回填），**降级为纯 CRM**：手工录入 + 线索池认领两条入口；访客不再自动写 agent_lead。
- **数据迁移**：存量 agent_lead 访客行 → lead_info（`VL+雪花id` 防跨渠道编码碰撞）；`agent_lead_trace` 按 trace_type 1/2/3 拆入三张记录表后 **DROP**。
- 留资链路：`/open/share/contact` 回填 lead_info 手机号/姓名 → `client_info.findOrCreateByPhone` 静默建客户档案 + 无密码账号（短信登录激活）→ 回填 `lead_info.client_code`。
- 认领链路：agent 端 `GET /leads/pool`（本渠道未认领）→ `POST /leads/claim/{visitorLeadCode}` 生成 agent_lead（防抢单校验）；admin 端线索池只读页 `/channel/lead`（权限 `lead:info:list/query`）。

**实际 lead 表（4）**：`lead_info`、`lead_content_read_record`、`lead_tool_use_record`、`lead_poster_view_record`。

---

## 三、业务模型与流程变更（对照 `01 产品需求` / `05 API`）

### 3.1 权益生命周期重构

设计文档描述"权益模板 → 批量印刷生成 → 入库"模型。**实际已重构**：
- 废除 `equity_template`，权益属性由 **`goods_equity`（商品配置）+ `service_item`（服务能力）** 驱动。
- 激活链路从 `template_code` 改挂 `goods_code`；`equity_depot` 激活时固化商品快照（`person_count`/`valid_days`）。
- 权益 `equity_type` 仅剩**展示用途**，唯一功能耦合是激活时 `mapEquityTypeToServiceType()` 决定 `service_session.service_type`；订单/履约不依赖。

### 3.2 配额履约模型

- 配额按 `equity + service_item` 聚合统计，不再用 `service_session` 单行 `used_count` 累加（单行固定为 1）。
- 关键修复：① 配额消费须在 `@TransactionalEventListener(AFTER_COMMIT)` 事务边界；② `ensureInUse` 状态防抖。
- client 端发起履约 + `usePersonId` 归属校验；`QuotaResetScheduler` 年度重置。

### 3.2a 权益内容建模（迁移 60，2026-08-15）

对照《大雁养老养老权益文档》三类产品（终身养老权益 6 档 / 旅居随心住 / 一年期养老管家）补全权益内容结构化：

- **人数两概念拆分**：权益人数量/构成 → `goods_equity`（`holder_rule` JSON：self/spouse/parent/designateAtActivation，`person_count`=构成之和自动校验）；单次服务使用人数/间数/晚数 → `goods_service_item_rel.usage_rule`（随心住：3天2晚/1间/每间2人/提前15天/预定金500/72h-48h-24h退订/春节9天黑名单）。随心住权益人仅本人、单次可住 2 人，两字段各表其一。
- **年度配额锚点改激活周年**：`quota_reset_year` 语义改为"权益周年序号"（`QuotaYears.benefitYear`，激活日起每满 12 个月 +1），修复一年期卡跨自然年配额翻倍（10次/年实得20次）。统计走 `countConsumedSessionsAnnualByPerson`。
- **终身语义**：`goods_equity.validity_type`（1=固定天数,2=终身），入库冻结到 `equity_depot`；终身激活 `expire_time=NULL`（`equity_activate.expire_time` 放开 NULL），客户端显示"终身有效"。
- **配额归属**：`goods_equity.share_mode`（0=按人独立,1=共享池）+ `service_session.use_person_id` 结构化落库（原先只拼 remark）；按人口径统计加 use_person_id 过滤。
- **入住权/折扣结构化**（rel 级，随商品档次差异化）：`admission_guaranteed/admission_priority/admission_discount` + `discount_rate`（90.00=门市价9折）；种子按文档修正——终身版三权全含、一年期管家旅居9折+长居照护优先优惠无保证。
- **服务网络范围 rel 级化**：`network_scope` JSON（NULL=业态全部在营机构；custom+parkCodes=自选子集），管理端机构多选替代裸 JSON textarea；消费点在客户端机构列表与安排校验（二期）。
- **权益人关系字典化**：`relation_with_holder` 存 code（self/spouse/parent/parent_in_law/child/other，字典 relation_with_holder），激活按构成建席位（本人+待填写-配偶/父母N），增改走 `validateRelationAgainstRule`（本人唯一/配偶≤1/父母≤4）。
- **可转让次数语义统一**：`max_transferable` 从 0/1 开关改为次数（0=不可,1/2/3=可转让N次，对齐文档）。
- **完成判定修正**：`EquityUsageEventListener` 仅在"全部为终身配额且用尽"时自动 complete；存在年度配额（会周年重置）或按人配额（单人不代表整卡）时永远 end_service 恢复激活，权益终结由过期/作废驱动。
- **随心住建模**：复用 goods_type=1 + 新服务项目 `SI00008 安排权益·旅居随心住入住` + 商品 `GD00010 单次版`(1次/权益期) / `GD00011 多次版`(3次/权益期，次数可调)，规则全量落 usage_rule，激活/履约链路复用。
- 管理端 `EquityConfigTab` 重构：权益期限/构成/共享/转让次数 + 子表展开行（入住权/折扣/网络范围/使用规则）；客户端权益详情结构化展示（入住权标签、折扣、随心住规则摘要、终身有效期、次/权益期文案）。

### 3.3 找机构三网络体系

设计文档未细分。**实际按活力/照护/旅居三网络物理分离**：
- 三目录：**vital（活力·蓝）/ care（照护·橙）/ sojourn（旅居·绿）**，各自独立列表/详情页。
- `park_info.network_tags`（逗号分隔多选）替代原 `ability_type`/`stay_type` 推导，`FIND_IN_SET` 精准查询。
- `vital_config`/`care_config`/`sojourn_config`（TEXT JSON）控制各网络头图/缩略图展示。
- **地图采用 DataV 动态边界 choropleth 热力图**（按 adcode 拉 GeoJSON + `useRegionHeatmap` composable），区县级起接入真实地图，已替换原 `effectScatter` 方案与 4.4MB 静态 GeoJSON 资产。

### 3.4 多方式登录（超出设计）

设计文档仅账号密码。**实际支持三方式登录**：验证码 / 密码 / 微信。
- `dayan-common-sms`（Mock + 开发态验证码）+ Redis 验证码（5min 有效 + 60s 冷却）。
- `identifier` 匹配 `phone`/`username`/`openId`；密码 BCrypt（`$2a$10$`）。
- Sa-Token 白名单 + 登录页 Tab 切换；微信为骨架扩展点。

### 3.5 内容多形式数据模型

`content_info` 支持 `content_type`（1图文/2视频/3图集/6文件）+ `source_type`（原创/转载）+ `content_body`（JSON 约定）；4 种卡片 + 详情按类型布局；`channel_config_content` 控可见性；`agent_favorite` 租户跳过。

### 3.6 Sa-Token 多端架构

单 `StpInterface` bean + SPI dispatcher 实现多端鉴权（多 bean 共存会启动崩）。每端 starter 启动时 `DefaultStpLogicInitializer` 调 `StpUtil.setStpLogic` 注册 `@SaCheckPermission`。client 端不用 `@SaCheckPermission`，权限保护下沉 service 层（`clientCode` 从 Sa-Token session 取）。

---

## 四、设计文档勘误清单（仅记录，不改原文）

以下为设计文档中**明确错误或互相矛盾**之处，原文保留不动，以此清单为准：

| # | 出处 | 问题 | 实际/正确 |
|---|---|---|---|
| 1 | 03 vs 06 | 调度框架矛盾（06: Quartz/XXL-Job；03: Spring @Scheduled） | Spring `@Scheduled`，无 Quartz/XXL-Job |
| 2 | 03 vs 06 | 父项目名矛盾（03: dayan-platform；06: dayan-server） | `dayan-server` |
| 3 | 03 vs 06 | 公共模块数矛盾（03: 8；06: 11） | 实际 12（含 bom；mq/lbs/pay/swagger 为占位） |
| 4 | 03 | 消息队列选型 RocketMQ | 未实际接入，`dayan-common-mq` 占位 |
| 5 | 01 头部 | 版本 v4.0 | 正文已含 v4.2/v4.3 修订，头部标签陈旧 |
| 6 | 02 头部 | 版本 v4.1 | 正文为 v4.2 |
| 7 | 01/02 | park 15 表（4 media + 5 price + facility/service_item） | 实际 13 表（见 §2.2） |
| 8 | 01/02 | equity 含 equity_template | equity_template 已 DROP（见 §2.3） |
| 9 | 01/02 | goods 含 goods_sku_* | SKU 重构（见 §2.4） |
| 10 | 01/02 | agent 6 表 | 实际 8 + learning_content（见 §2.6） |
| 11 | 07 v1.2 | finance_flow 5 类型 → 4 类型 | 以 4 类型为准（1收入/2支出/3退款/4结算） |
| 12 | 06 v1.1 | AES-256-GCM + 软删 `deleted`1/0 + `deleted_at` | 正确（旧文档若仍写 ECB/NOW() 以 v1.1 为准） |

---

## 五、迁移与种子现状

- `db/migration/`：编号 `01`~`44` 为顺序基线 + 迭代重构（38 记录表命名收口 / 39 日志四分（含存量分流）/ 40 管家 organ 账号关联 / 41 content_category 字典化 / 42 tool 域 / 43 lead 域 / 44 内容分类管理页下线；2026-08-14 起目录收敛：过期 ad-hoc 重构脚本 `facility_service_rename.sql` / `park_asset_merge.sql` / `park_pricing_refactor.sql` / `sync_assets.sql` / `sync_park_banner_config.sql` / `organ_permission_menu_code.sql` 已归档至 `db/archive/`，其结果已折叠进基础 DDL；老系统 ETL 脚本与 TSV 数据源移至 `db/tools/`；目录规约见 `db/migration/README.md`）。
- `db/migration/seed/`：`admin_seed` / `dict_seed` / `region_seed` / `menu_seed`(+channel) / `rbac_*` / `channel_permission_seed` / `state_machine_seed`。全部幂等（`ON DUPLICATE KEY UPDATE updated_at = updated_at`）；菜单历史增量（rename/restructure/move_back）已折叠进 `menu_seed.sql` 并归档 `db/archive/legacy_menu/`；`rbac_demo_seed.sql` 演示账号被 `99_seed.sh` 排除，仅手动执行。
- **20 家机构数据已从远程 `wkb_yl` 库 5 轮迁移完成**（供应商/机构/图片/周边/展示板块/房型/餐饮/基本信息/结构化价格 97 条/素材库 457 张）。`wkb_yl` 的服务/设施/phone 字段全 NULL 不可迁。
- 远程参考库连接：`onlineread.rwlb.rds.aliyuncs.com`（线上养老项目 DB）。

---

## 六、变更历史

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-08-13 | v1.0 | 首版：对照设计文档 v4.x，记录架构/调度/5 域表结构/业务模型/勘误清单实现现状 |
| 2026-08-14 | v1.1 | 增补迁移 38–43：命名收口、日志四分、管家 organ 账号关联、分类字典化、tool/lead 两新域（§2.7–2.12） |
| 2026-08-15 | v1.2 | 迁移 60 权益内容建模：人数两概念拆分（holder_rule/usage_rule）、年度配额改激活周年、终身语义、share_mode 按人配额、入住权/折扣/网络范围结构化、关系字典化、随心住商品（§3.2a） |
